apiVersion: apps/v1
kind: Deployment
metadata:
  name: analysis-service
  namespace: vifinancenews
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analysis-service
  template:
    metadata:
      labels:
        app: analysis-service
    spec:
      containers:
        - name: analysis-service
          image: dattran453/analysis_service:v1.0.0
          ports:
            - containerPort: 7003
          envFrom:
            - configMapRef:
                name: vifinancenews-config
            - secretRef:
                name: vifinancenews-secret
            - secretRef:
                name: vittel-s3-credentials
          volumeMounts:
            - mountPath: /app/models/hub
              name: hf-models
      volumes:
          - name: hf-models
            persistentVolumeClaim:
              claimName: hf-models-pvc
            emptyDir: {}