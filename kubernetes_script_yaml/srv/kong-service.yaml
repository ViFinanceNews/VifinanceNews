apiVersion: v1
kind: Service
metadata:
  name: kong
  namespace: vifinancenews
spec:
  selector:
    app: kong
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
    - protocol: TCP
      port: 8001
      targetPort: 8001